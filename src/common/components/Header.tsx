import React from "react"
import Button from "@mui/material/Button"
type Props = {}

const Header = (props: Props) => {
    return (
        <div>
            <div>Hello world in header</div>
            <Button variant="contained">Hello World</Button>
        </div>
    )
}

export default Header
